import React from 'react';
import { Text } from 'react-native-paper';

export const ModalList = ({ type, title, name, extraFieldsObj, toggleExtraFieldsObj }) => {
    return (<Text>Modal List</Text>)
}